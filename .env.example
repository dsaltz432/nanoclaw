TELEGRAM_BOT_TOKEN=

# Model for agent containers: an alias ("opus", "sonnet") that follows the
# tier's current model, or an exact id. A group's containerConfig.model
# overrides it. Unset = the Agent SDK default (Sonnet).
# CLAUDE_MODEL=opus
